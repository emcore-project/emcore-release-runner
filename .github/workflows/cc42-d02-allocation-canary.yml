name: CC42 D02 Allocation Canary

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cc42-d02-allocation-canary.yml'

permissions:
  contents: read

concurrency:
  group: emcore-cc42-d02-allocation-canary
  cancel-in-progress: false

jobs:
  allocation-canary:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Prove runner allocation
        run: |
          set -euo pipefail
          echo "CC42_D02_ALLOCATION_CANARY_PASS"
